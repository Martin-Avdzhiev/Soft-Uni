function power(number, multiply) {

    function multiplyer(number, multiply) {
        let poweredNumber = number ** multiply
        
        return poweredNumber;

    }

    let result = multiplyer(number,multiply);
    console.log(result);
}

power(2, 8)