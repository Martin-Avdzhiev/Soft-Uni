function solve(first, second, third) {
    const object = {
        name: first,
        population: Number(second),
        treasury: Number(third)
    }
    return object;
}

solve('Tortuga', 7000, 15000)