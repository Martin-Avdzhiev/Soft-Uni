import { MultiplyPipePipe } from './multiply-pipe.pipe';

describe('MultiplyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
