import { CryptoDirectiveDirective } from './crypto-directive.directive';

describe('CryptoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CryptoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
